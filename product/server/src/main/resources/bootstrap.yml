eureka:
  client:
    service-url:
      defaultZone: http://localhost:4321/eureka
spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
